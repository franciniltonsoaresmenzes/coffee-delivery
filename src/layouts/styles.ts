import styled from 'styled-components'

export const DefaultLayoutContainer = styled.main`
  .container {
    max-width: 1140px;
    margin: auto;
    padding-left: 10px;
    padding-right: 10px;
  }
`
